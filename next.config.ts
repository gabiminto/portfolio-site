import type {NextConfig} from "next";

const nextConfig: NextConfig = {
    // Uncomment the line below if deploying to GitHub Pages with static export
    // output: 'export',
};

export default nextConfig;
